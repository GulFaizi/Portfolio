{
    "name": "portfolio-root",
    "version": "1.0.0",
    "private": true,
    "description": "Root configuration for frontend deployment",
    "scripts": {
        "postinstall": "cd client && npm install",
        "start": "cd client && npm run dev",
        "build": "cd client && npm run build"
    },
    "engines": {
        "node": ">=16.0.0"
    },
    "dependencies": {
        "pdf-parse": "^2.4.5",
        "pdf2json": "^4.0.2"
    }
}
